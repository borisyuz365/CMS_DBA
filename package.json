{
  "name": "athlete-management-system",
  "version": "1.0.0",
  "description": "Full-stack Athletes Management System",
  "scripts": {
    "both": "concurrently -n \"BACKEND,FRONTEND,BP\" -c \"blue,green,magenta\" \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:bp\"",
    "dev:backend": "cd backend && npm start",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:bp": "cd bp-service && npm run dev",
    "kill": "node ./scripts/kill-dev-processes.js",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../bp-service && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
